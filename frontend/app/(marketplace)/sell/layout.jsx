// Seller onboarding (Razorpay KYC) — private, behind login. Keep out of indexes.
export const metadata = {
  title: 'Set up selling',
  robots: { index: false, follow: false },
}

export default function SellLayout({ children }) {
  return children
}
